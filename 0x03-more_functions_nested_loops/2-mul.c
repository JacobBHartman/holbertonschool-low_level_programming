#include "holberton.h"
/**
 * mul - multiplies two integeres
 * @a: first factor to be mulitplied
 * @b: second factor to be multiplied
 * Return: returns the products
 */
int mul(int a, int b)
{
	return (a * b);
}
