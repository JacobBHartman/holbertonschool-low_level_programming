#include "holberton.h"
/**
 * more_numbers - prints numbers 0 thru 14, ten times
 *
 * Return: void
 */
void more_numbers(void)
{
	int i;
	int j;

	for (j = 0; j < 10; j++)
	{
		for (i = 0; i < 15; i++)
		{
			_putchar('0' + (i % 10));
			if (i > 9)
				_putchar('0' + (i / 10));
		}
		_putchar('\n');
	}
}
