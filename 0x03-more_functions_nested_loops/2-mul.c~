#include "holberton.h"
/**
 * _isdigit - checks if is digit 0-9
 * @c: the variable that is to be checked if digit
 * Return: if digit return 1; otherwise return 0.
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	else
		return (0);
}
