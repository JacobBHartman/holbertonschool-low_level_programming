#include "holberton.h"

/**
 * wildcmp - compares two strings to see if theyre identical incl. wildcards
 * @s1: first string
 * @s2: second string
 *
 * Return: 1 if identical, 0 otherwise
 */
int wildcmp(char *s1, char *s2)
{
	int i;
	int j;
	int lens2;
	unsigned int matching;

	matching = 1;
	// strlen s1len
	// strlen s2len
	for (i = 0; s1[i] != '\0'; i++)
	{
		
	}
}
