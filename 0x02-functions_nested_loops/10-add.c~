#include "holberton.h"

/**
 * add - adds two integers and returns them
 * @int: first int to be added
 * @int: second int to be added
 * Return: an int that is the sum of two other ints
 */
int add(int x, int y)
{
	int result;

	result = x + y;
	return (result);
}
